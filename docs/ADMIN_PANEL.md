# 💎 BananaBot Admin Panel

Веб-интерфейс для управления пакетами кредитов бота.

![Admin Panel](https://img.shields.io/badge/Next.js-15.0-black?logo=next.js)
![TypeScript](https://img.shields.io/badge/TypeScript-5.0-blue?logo=typescript)
![Prisma](https://img.shields.io/badge/Prisma-6.17-2D3748?logo=prisma)

## 🚀 Быстрый старт

### 1. Установка

```bash
# Из корня проекта
make web-install
```

### 2. Настройка

Файл `.env` уже создан в `web/.env` с настройками по умолчанию:

```env
DATABASE_URL="file:../src/prisma/dev.db"
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
```

⚠️ **Важно**: Измените `ADMIN_PASSWORD` перед деплоем!

### 3. Создание тестовых пакетов

```bash
cd web
npm run seed
```

Это создаст 4 тестовых пакета:
- ✅ **Starter** - 10 кредитов за 99 руб.
- ⭐ **Pro** - 50 кредитов за 399 руб. (скидка 20%)
- 💎 **Ultimate** - 150 кредитов за 999 руб. (скидка 30%)
- ❌ **Test** - тестовый пакет (неактивен)

### 4. Запуск

```bash
# Development mode
make web-dev

# Production mode
make web-build
make web-start
```

Админ-панель будет доступна: **http://localhost:3001**

## 📋 Функции

### Управление пакетами

- ✅ **Просмотр** всех пакетов кредитов
- ✅ **Создание** новых пакетов
- ✅ **Редактирование** существующих пакетов
- ✅ **Удаление** пакетов
- ✅ **Активация/деактивация** пакетов

### Настройка цен

Для каждого пакета можно установить цены для разных платежных систем:

- 💳 **YooMoney** (рубли)
- ⭐ **Telegram Stars**
- ₿ **Криптовалюта** (USDT)

### Дополнительные опции

- 🏷️ **Скидки** - процент скидки
- ⭐ **Популярные пакеты** - отметка для выделения
- ✅ **Статус активности** - включение/выключение пакета
- 📝 **Описание** - текстовое описание пакета

## 📸 Скриншоты

### Список пакетов

```
┌─────────────────────────────────────────────────────────┐
│ 💎 BananaBot Admin - Credit Packages   [+ Add Package] │
├─────────────────────────────────────────────────────────┤
│ Name          Credits  Price     Discount  Status       │
├─────────────────────────────────────────────────────────┤
│ Pro ⭐         50       399 руб.  20%       ✅ Active    │
│ Starter       10       99 руб.   -         ✅ Active    │
│ Ultimate      150      999 руб.  30%       ✅ Active    │
└─────────────────────────────────────────────────────────┘
```

### Форма создания/редактирования

```
┌─────────────────────────────┐
│ Create Package              │
├─────────────────────────────┤
│ Package Name: [Starter    ] │
│ Credits:      [10         ] │
│ Base Price:   [100        ] │
│ Currency:     [RUB ▼]       │
│                             │
│ YooMoney Price: [99       ] │
│ Telegram Stars: [100      ] │
│ Crypto Price:   [         ] │
│ Discount (%):   [0        ] │
│                             │
│ Description:                │
│ [Попробуйте наш сервис]     │
│                             │
│ ☑ Mark as Popular           │
│ ☑ Active                    │
│                             │
│ [Cancel] [Create Package]   │
└─────────────────────────────┘
```

## 🔧 API Endpoints

### Packages

| Method | Endpoint               | Description                |
|--------|------------------------|----------------------------|
| GET    | `/api/packages`        | Получить все пакеты        |
| POST   | `/api/packages`        | Создать новый пакет        |
| GET    | `/api/packages/[id]`   | Получить конкретный пакет  |
| PUT    | `/api/packages/[id]`   | Обновить пакет             |
| DELETE | `/api/packages/[id]`   | Удалить пакет              |

### Пример запроса

```bash
# Создать новый пакет
curl -X POST http://localhost:3001/api/packages \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Starter",
    "credits": 10,
    "price": 100,
    "priceYooMoney": 99,
    "priceStars": 100,
    "popular": false,
    "active": true
  }'
```

## 📁 Структура проекта

```
web/
├── app/
│   ├── api/
│   │   └── packages/
│   │       ├── route.ts          # GET, POST /api/packages
│   │       └── [id]/
│   │           └── route.ts      # GET, PUT, DELETE /api/packages/[id]
│   ├── layout.tsx                # Корневой layout
│   ├── page.tsx                  # Главная страница админки
│   └── globals.css               # Глобальные стили
├── lib/
│   ├── prisma.ts                 # Prisma client
│   └── auth.ts                   # Аутентификация (базовая)
├── scripts/
│   └── seed-packages.ts          # Seed скрипт
├── package.json
├── tsconfig.json
├── next.config.ts
├── .env                          # Настройки окружения
├── .env.example
├── README.md
└── SETUP.md
```

## 🔒 Безопасность

### Текущая реализация

✅ Environment-based аутентификация
✅ Отдельный порт (3001)
✅ Простая валидация

### Рекомендации для Production

⚠️ **Обязательно**:

1. **Сильный пароль** в `ADMIN_PASSWORD`
2. **HTTPS** для production
3. **IP whitelist** для доступа к админке
4. **Rate limiting** на API endpoints

🔜 **Опционально**:

1. Использовать **NextAuth.js** для полноценной аутентификации
2. Добавить **RBAC** (Role-Based Access Control)
3. Включить **логирование** всех действий
4. Добавить **2FA** (Two-Factor Authentication)
5. Использовать **JWT** для сессий

### Пример улучшенной аутентификации

```typescript
// Будущая реализация с NextAuth.js
import NextAuth from 'next-auth';
import CredentialsProvider from 'next-auth/providers/credentials';

export default NextAuth({
  providers: [
    CredentialsProvider({
      credentials: {
        username: { label: "Username", type: "text" },
        password: { label: "Password", type: "password" }
      },
      authorize: async (credentials) => {
        // Ваша логика проверки
      }
    })
  ]
});
```

## 🐛 Troubleshooting

### Пакеты не отображаются в боте

**Проблема**: После создания пакетов в админке, они не видны в боте.

**Решения**:
1. Проверьте, что пакет помечен как **Active** (✅)
2. Перезапустите бот: `make restart`
3. Проверьте логи бота на ошибки

### Ошибка подключения к базе данных

**Проблема**: `Error: Can't reach database server`

**Решения**:
1. Убедитесь, что `DATABASE_URL` в `web/.env` правильный
2. Проверьте, что PostgreSQL запущен (или SQLite файл существует)
3. Проверьте права доступа к базе данных

### Порт 3001 занят

**Проблема**: `Error: listen EADDRINUSE: address already in use :::3001`

**Решения**:
1. Убейте процесс: `lsof -ti:3001 | xargs kill -9`
2. Или измените порт в `package.json`:
   ```json
   "dev": "next dev -p 3002"
   ```

### Изменения не применяются

**Проблема**: После редактирования пакета изменения не видны.

**Решения**:
1. Проверьте консоль браузера на ошибки
2. Обновите страницу (Ctrl+R / Cmd+R)
3. Проверьте, что запрос прошел успешно (Network tab в DevTools)

## 📊 Статистика и мониторинг

### Будущие улучшения

- [ ] **Dashboard** с графиками продаж
- [ ] **История изменений** пакетов
- [ ] **Статистика покупок** по пакетам
- [ ] **Экспорт данных** в CSV/Excel
- [ ] **Real-time обновления** через WebSocket
- [ ] **Email уведомления** о покупках
- [ ] **A/B тестирование** пакетов

### Пример будущего dashboard

```
┌─────────────────────────────────────────┐
│ 📊 Dashboard                            │
├─────────────────────────────────────────┤
│ 💰 Revenue Today:      12,450 руб.     │
│ 📦 Packages Sold:      47               │
│ 👥 Active Users:       1,234            │
│                                         │
│ 📈 Top Packages:                        │
│   1. Pro ⭐          32 sales           │
│   2. Starter         12 sales           │
│   3. Ultimate         3 sales           │
│                                         │
│ [View Full Report]                      │
└─────────────────────────────────────────┘
```

## 🚢 Деплой

### Vercel (рекомендуется)

```bash
# Установите Vercel CLI
npm i -g vercel

# Деплой
cd web
vercel
```

### Docker

```dockerfile
# Dockerfile для админки
FROM node:22-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM node:22-alpine
WORKDIR /app
COPY --from=builder /app/.next/standalone ./
COPY --from=builder /app/.next/static ./.next/static
EXPOSE 3001
CMD ["node", "server.js"]
```

### Nginx reverse proxy

```nginx
server {
    listen 80;
    server_name admin.bananabot.com;

    location / {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

## 📚 Дополнительные ресурсы

- [Next.js Documentation](https://nextjs.org/docs)
- [Prisma Documentation](https://www.prisma.io/docs)
- [React Documentation](https://react.dev)
- [TypeScript Documentation](https://www.typescriptlang.org/docs)

## 🤝 Contributing

Для добавления новых функций в админ-панель:

1. Создайте feature branch
2. Добавьте функционал
3. Обновите документацию
4. Создайте Pull Request

## 📄 License

MIT
