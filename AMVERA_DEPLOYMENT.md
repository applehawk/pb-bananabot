# 🚀 Развертывание BananaBot на Amvera

Это подробное руководство по развертыванию BananaBot (Telegram бот + Admin Panel) на платформе Amvera.

## 📋 Содержание

- [О платформе Amvera](#о-платформе-amvera)
- [Предварительная подготовка](#предварительная-подготовка)
- [Шаг 1: Создание PostgreSQL базы данных](#шаг-1-создание-postgresql-базы-данных)
- [Шаг 2: Подготовка репозитория](#шаг-2-подготовка-репозитория)
- [Шаг 3: Развертывание Telegram Bot](#шаг-3-развертывание-telegram-bot)
- [Шаг 4: Развертывание Admin Panel](#шаг-4-развертывание-admin-panel)
- [Шаг 5: Настройка доменов](#шаг-5-настройка-доменов-опционально)
- [Мониторинг и отладка](#мониторинг-и-отладка)
- [Обновление приложения](#обновление-приложения)
- [Troubleshooting](#troubleshooting)

---

## О платформе Amvera

[Amvera](https://amvera.ru/) — это российская PaaS-платформа для развертывания приложений с автоматической сборкой из Git репозиториев.

**Ключевые особенности:**

✅ Managed PostgreSQL с автоматическими бэкапами
✅ Автоматические SSL сертификаты для HTTPS
✅ Интеграция с Git (GitHub, GitLab, Bitbucket)
✅ Автоматическая сборка и развертывание при push
✅ Внутренняя сеть для связи между сервисами
✅ Бесплатный тарифный план для небольших проектов

**Документация Amvera:**
- [Общая документация](https://docs.amvera.ru/)
- [PostgreSQL](https://docs.amvera.ru/databases/postgreSQL.html)
- [Сборка приложений](https://docs.amvera.ru/applications/build.html)
- [Запуск приложений](https://docs.amvera.ru/applications/run.html)
- [Сетевая конфигурация](https://docs.amvera.ru/applications/configuration/network.html)

---

## Предварительная подготовка

### 1. Создание аккаунта

1. Перейдите на [amvera.ru](https://amvera.ru/)
2. Зарегистрируйтесь или войдите через GitHub/GitLab

### 2. Подготовка переменных окружения

Подготовьте следующие данные (они понадобятся при настройке):

**Для Telegram Bot:**
```bash
BOT_TOKEN=<получите у @BotFather>
ADMIN_CHAT_ID=<ваш Telegram ID, получите у @userinfobot>
YOOMONEY_SECRET=<секрет от YooMoney, если используете>
TELEGRAM_SECRET_TOKEN=<случайная строка для безопасности webhook>
```

**Для Admin Panel:**
```bash
NEXTAUTH_SECRET=<случайная строка минимум 32 символа>
```

Для генерации `NEXTAUTH_SECRET`:
```bash
openssl rand -base64 32
```

### 3. Убедитесь, что репозиторий готов

```bash
# Проверьте, что submodules инициализированы
git submodule status

# Если нет, инициализируйте
git submodule update --init --recursive

# Проверьте наличие необходимых файлов
ls -la Dockerfile
ls -la docker-compose.amvera.yml
ls -la bananabot-admin/Dockerfile
```

---

## Шаг 1: Создание PostgreSQL базы данных

### 1.1. Создание кластера

1. Войдите в [Amvera Dashboard](https://amvera.ru/dashboard)
2. Нажмите кнопку **"+ Создать"** → **"PostgreSQL"**
3. Заполните параметры:
   - **Название проекта**: `bananabot-db` (или любое другое)
   - **Регион**: выберите ближайший к вашим пользователям
   - **Тарифный план**: минимум **"Starter"** (рекомендуется)
   - **Версия PostgreSQL**: 16 (или последняя доступная)

4. Настройте параметры БД:
   - **Database Name**: `bananabot`
   - **Username**: `bananabot_user` (или любой другой)
   - **Password**: создайте надежный пароль (сохраните!)

5. Нажмите **"Создать базу данных"**

### 1.2. Получение строки подключения

После создания БД вы получите параметры подключения:

**Для внутренних сервисов (Bot и Admin на Amvera):**
```
Host: amvera-<username>-cnpg-bananabot-db-rw
Port: 5432
Database: bananabot
Username: bananabot_user
Password: <ваш пароль>
```

**Строка подключения (DATABASE_URL):**
```
postgresql://bananabot_user:<password>@amvera-<username>-cnpg-bananabot-db-rw:5432/bananabot?schema=public
```

**Для внешнего доступа (если нужно):**
```
Host: bananabot-db.<username>.db-<region>.amvera.tech
Port: 5432
```

⚠️ **Важно**: Сохраните эти данные! Они понадобятся для настройки приложений.

### 1.3. Настройка доменного имени для внешнего доступа (опционально)

Если вам нужен доступ к БД извне (например, для Prisma Studio):

1. В настройках БД создайте **Domain Name**
2. Тип: **POSTGRES**
3. SSL: **Обязательно включено**

---

## Шаг 2: Подготовка репозитория

### 2.1. Проверка структуры

Убедитесь, что ваш репозиторий имеет следующую структуру:

```
bananabot/
├── prisma/                    # Submodule
├── bananabot-admin/           # Submodule
├── Dockerfile                 # Для бота
├── docker-compose.amvera.yml  # Для Amvera (опционально)
├── .gitmodules
└── ... остальные файлы
```

### 2.2. Обновление submodules

На Amvera при клонировании репозитория Git не инициализирует submodules автоматически.

**Решение**: Убедитесь, что ваш `Dockerfile` содержит команды для инициализации submodules:

**В корневом Dockerfile (для бота):**
```dockerfile
# В стадии builder, после WORKDIR /app
RUN apk add --no-cache git

# Перед копированием prisma
COPY .gitmodules .gitmodules
RUN git submodule update --init --recursive prisma || true

# Копируем prisma submodule
COPY prisma ./prisma
```

**Альтернатива**: Используйте GitHub Actions для автоматической сборки с submodules.

### 2.3. Проверка .dockerignore

Убедитесь, что `.dockerignore` не исключает важные файлы:

```
# .dockerignore
node_modules
npm-debug.log
.env
.git
!.gitmodules
dist
.next
```

⚠️ Обратите внимание на `!.gitmodules` — это важно для работы с submodules.

---

## Шаг 3: Развертывание Telegram Bot

### 3.1. Создание проекта

1. В Amvera Dashboard нажмите **"+ Создать"** → **"Application"**
2. Выберите источник: **"Git Repository"**
3. Подключите ваш репозиторий (GitHub/GitLab/Bitbucket)
4. Выберите ветку: обычно **`main`** или **`master`**

### 3.2. Настройка конфигурации

**Метод 1: Использование Dockerfile (рекомендуется)**

Amvera автоматически обнаружит `Dockerfile` в корне проекта.

1. Build context: **`/`** (корень репозитория)
2. Dockerfile path: **`Dockerfile`**

**Метод 2: Использование amvera.yml**

Если хотите использовать `amvera.yml` вместо Dockerfile:

```yaml
# amvera.yml
meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  cmds:
    - npm install
    - cd prisma && npm install && npx prisma generate
    - npm run build

run:
  cmds:
    - cd prisma && npx prisma migrate deploy
    - node dist/main.js
  containerPort: 80
```

### 3.3. Настройка переменных окружения

В разделе **"Environment Variables"** добавьте:

```bash
# Node
NODE_ENV=production

# Database
DATABASE_URL=postgresql://bananabot_user:<password>@amvera-<username>-cnpg-bananabot-db-rw:5432/bananabot?schema=public

# Telegram Bot
BOT_TOKEN=<ваш токен от BotFather>
ADMIN_CHAT_ID=<ваш Telegram ID>

# Server
PORT=80
DOMAIN=https://<ваш-проект>.amvera.io

# Payment
YOOMONEY_SECRET=<ваш секрет>

# Security
TELEGRAM_SECRET_TOKEN=<случайная строка>

# Optional
ADMIN_CHAT_ID_2=<второй админ ID>
REDIS_URL=<если используете Redis>
```

⚠️ **Важно**:
- `PORT=80` — Amvera ожидает, что приложение слушает порт 80 по умолчанию
- `DOMAIN` будет доступен после первой сборки (вида `<project>.<username>.amvera.io`)

### 3.4. Настройка сети

В разделе **"Network"**:

1. **Container Port**: `80` (порт, на котором слушает приложение)
2. **Service Port**: `80` (порт для внутренней сети)
3. **Public Access**: **Включено** (для webhook от Telegram)

### 3.5. Запуск сборки

1. Нажмите **"Create Application"** или **"Build"**
2. Amvera начнет сборку Docker образа
3. Следите за логами сборки в разделе **"Build Log"**

**Процесс сборки:**
```
1. Git clone репозитория
2. Обнаружение Dockerfile
3. Сборка Docker образа
4. Применение миграций Prisma (если настроено в CMD)
5. Запуск приложения
```

### 3.6. Настройка домена

После успешной сборки приложение будет доступно по адресу:
```
https://<project-name>.<username>.amvera.io
```

Для использования собственного домена см. [Шаг 5](#шаг-5-настройка-доменов-опционально).

### 3.7. Установка Telegram Webhook

После развертывания настройте webhook для Telegram:

**Метод 1: Через API Telegram**

```bash
curl -X POST "https://api.telegram.org/bot<BOT_TOKEN>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://<your-project>.amvera.io/webhook",
    "secret_token": "<TELEGRAM_SECRET_TOKEN>"
  }'
```

**Метод 2: Через скрипт в проекте**

Если у вас есть `npm run webhook:set`, выполните его локально с правильными переменными окружения:

```bash
BOT_TOKEN=<your_token> DOMAIN=https://<your-project>.amvera.io npm run webhook:set
```

**Проверка webhook:**
```bash
curl "https://api.telegram.org/bot<BOT_TOKEN>/getWebhookInfo"
```

---

## Шаг 4: Развертывание Admin Panel

Admin Panel развертывается как отдельный проект на Amvera.

### 4.1. Создание проекта

1. В Amvera Dashboard создайте новый проект **"+ Создать"** → **"Application"**
2. Подключите тот же Git репозиторий
3. Выберите ту же ветку

### 4.2. Настройка конфигурации

**Build Context и Dockerfile:**

Поскольку Admin Panel находится в submodule `bananabot-admin`, нужно правильно настроить пути:

**Вариант 1: Dockerfile в субдиректории (рекомендуется для Amvera)**

Создайте отдельную ветку или используйте специальный Dockerfile:

1. Build context: **`/`**
2. Dockerfile path: **`bananabot-admin/Dockerfile`**

**Вариант 2: Использование amvera.yml**

```yaml
# amvera.yml (для admin panel)
meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  workdir: bananabot-admin
  cmds:
    - pnpm install
    - cd prisma && pnpm install && pnpm generate
    - pnpm run build

run:
  workdir: bananabot-admin
  cmds:
    - cd prisma && pnpm migrate
    - pnpm start
  containerPort: 80
```

**Вариант 3: Создание отдельного репозитория для Admin Panel**

Самый простой способ — развернуть `bananabot-admin` как отдельный репозиторий:

```bash
# Локально
cd bananabot-admin
git remote add amvera-admin <amvera-git-url-for-admin>
git push amvera-admin main
```

### 4.3. Настройка переменных окружения

```bash
# Node
NODE_ENV=production

# Database (та же, что у бота)
DATABASE_URL=postgresql://bananabot_user:<password>@amvera-<username>-cnpg-bananabot-db-rw:5432/bananabot?schema=public

# Next.js Server
PORT=80
HOSTNAME=0.0.0.0

# NextAuth
NEXTAUTH_SECRET=<случайная строка 32+ символов>
NEXTAUTH_URL=https://<admin-project>.amvera.io

# Optional: Bot API
BOT_API_URL=https://<bot-project>.amvera.io
```

### 4.4. Настройка сети

1. **Container Port**: `80`
2. **Service Port**: `80`
3. **Public Access**: **Включено**

### 4.5. Запуск и проверка

1. Запустите сборку
2. Проверьте логи сборки
3. После успешной сборки откройте админ-панель:
   ```
   https://<admin-project>.<username>.amvera.io
   ```

---

## Шаг 5: Настройка доменов (опционально)

Для использования собственных доменов вместо `*.amvera.io`:

### 5.1. Настройка DNS записей

В настройках вашего регистратора домена создайте следующие записи:

**Для основного домена (например, bot.yourdomain.com):**

| Тип | Имя | Значение | TTL |
|-----|-----|----------|-----|
| A | bot | <IP из Amvera> | 3600 |
| TXT | _acme-challenge.bot | <код из Amvera> | 3600 |

**Для админ-панели (например, admin.yourdomain.com):**

| Тип | Имя | Значение | TTL |
|-----|-----|----------|-----|
| A | admin | <IP из Amvera> | 3600 |
| TXT | _acme-challenge.admin | <код из Amvera> | 3600 |

### 5.2. Привязка домена в Amvera

1. Откройте настройки проекта в Amvera
2. Перейдите в раздел **"Domains"**
3. Нажмите **"Add Domain"**
4. Введите ваш домен: `bot.yourdomain.com`
5. Следуйте инструкциям для верификации

### 5.3. Ожидание SSL сертификата

Amvera автоматически выпустит SSL сертификат от Let's Encrypt:

- Проверка DNS: до 24 часов
- Выпуск сертификата: несколько минут
- Автоматическое обновление каждые 90 дней

### 5.4. Обновление webhook (для бота)

После привязки домена обновите webhook:

```bash
curl -X POST "https://api.telegram.org/bot<BOT_TOKEN>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://bot.yourdomain.com/webhook",
    "secret_token": "<TELEGRAM_SECRET_TOKEN>"
  }'
```

---

## Мониторинг и отладка

### Просмотр логов

**В Amvera Dashboard:**

1. Откройте проект
2. Перейдите в раздел **"Application Log"**
3. Нажмите кнопку обновления для загрузки последних логов

**Типы логов:**
- **Build Log**: логи сборки Docker образа
- **Application Log**: runtime логи приложения

### Health Checks

Убедитесь, что ваши приложения имеют health check endpoints:

**Для бота:**
```typescript
// src/main.ts или отдельный controller
@Get('/health')
health() {
  return { status: 'ok', timestamp: new Date().toISOString() };
}
```

**Для админ-панели:**
```typescript
// app/api/health/route.ts
export async function GET() {
  return Response.json({ status: 'ok' });
}
```

### Мониторинг базы данных

В Amvera Dashboard для PostgreSQL:

1. **Metrics**: CPU, Memory, Disk usage
2. **Backups**: автоматические бэкапы (последние 3)
3. **Connections**: активные подключения

### Проверка статуса сервисов

```bash
# Проверка бота
curl https://<bot-project>.amvera.io/health

# Проверка админ-панели
curl https://<admin-project>.amvera.io/api/health

# Проверка БД (если настроен внешний доступ)
psql "postgresql://bananabot_user:<password>@bananabot-db.<username>.db-<region>.amvera.tech:5432/bananabot" -c "SELECT version();"
```

---

## Обновление приложения

### Автоматическое развертывание

Amvera автоматически пересобирает приложение при push в master/main:

```bash
# Внесите изменения
git add .
git commit -m "feat: new feature"

# Push в GitHub/GitLab
git push origin main

# Amvera автоматически начнет пересборку
```

### Ручное развертывание

В Amvera Dashboard:

1. Откройте проект
2. Нажмите **"Rebuild Project"**
3. Следите за логами сборки

### Обновление переменных окружения

1. Откройте настройки проекта
2. Измените переменные в разделе **"Environment Variables"**
3. Нажмите **"Save"**
4. **Перезапустите** приложение (Restart)

### Миграции базы данных

Миграции Prisma применяются автоматически при запуске:

**В Dockerfile:**
```dockerfile
CMD ["sh", "-c", "cd prisma && npx prisma migrate deploy && cd .. && node dist/main.js"]
```

**Ручное применение миграций (если нужно):**

```bash
# Подключитесь к БД через pgAdmin или psql
# Или используйте временный контейнер на Amvera
```

---

## Troubleshooting

### Проблема: Submodules не инициализируются

**Симптомы**: Ошибка при сборке "prisma not found"

**Решение**: Добавьте инициализацию submodules в Dockerfile:

```dockerfile
# В стадии builder
RUN apk add --no-cache git
COPY .gitmodules .gitmodules
RUN git init && git submodule update --init --recursive || true
```

### Проблема: База данных недоступна

**Симптомы**: "Connection refused" или timeout

**Проверка**:
1. Правильность `DATABASE_URL`
2. Формат внутреннего домена: `amvera-<username>-cnpg-<db-project>-rw`
3. Порт: `5432`

**Решение**: Используйте точный домен из настроек БД в Amvera Dashboard.

### Проблема: Webhook не работает

**Симптомы**: Бот не отвечает на сообщения

**Проверка**:
```bash
curl "https://api.telegram.org/bot<BOT_TOKEN>/getWebhookInfo"
```

**Решение**:
1. Убедитесь, что webhook установлен правильно
2. Проверьте, что приложение слушает на порту 80
3. Проверьте логи приложения на наличие ошибок

### Проблема: Превышен лимит памяти

**Симптомы**: Приложение падает с "Out of Memory"

**Решение**:
1. Оптимизируйте Dockerfile (multi-stage build)
2. Увеличьте тарифный план Amvera
3. Оптимизируйте код приложения

### Проблема: Долгая сборка

**Симптомы**: Сборка занимает более 10 минут

**Решение**:
1. Используйте кеширование слоев Docker
2. Оптимизируйте порядок команд в Dockerfile
3. Используйте `.dockerignore`

---

## Полезные ссылки

- [Документация Amvera](https://docs.amvera.ru/)
- [PostgreSQL на Amvera](https://docs.amvera.ru/databases/postgreSQL.html)
- [Docker best practices](https://docs.docker.com/develop/dev-best-practices/)
- [Prisma Migrations](https://www.prisma.io/docs/concepts/components/prisma-migrate)
- [Next.js Deployment](https://nextjs.org/docs/deployment)

---

## Заключение

Развертывание на Amvera — это простой и быстрый способ запустить ваш проект в production. Следуя этому руководству, вы сможете:

✅ Развернуть полнофункциональный Telegram бот
✅ Запустить админ-панель на Next.js
✅ Настроить managed PostgreSQL с автоматическими бэкапами
✅ Получить автоматические SSL сертификаты
✅ Настроить автоматическое развертывание при push

Если у вас возникли вопросы или проблемы, обращайтесь к [документации Amvera](https://docs.amvera.ru/) или создайте Issue в GitHub репозитории проекта.

**Удачного развертывания!** 🚀
